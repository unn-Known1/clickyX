{
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "overlay"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "global-shortcut:default"
  ]
}
